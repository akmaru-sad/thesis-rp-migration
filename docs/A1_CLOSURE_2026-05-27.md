# A.1 CLOSURE LOG — 2026-05-27

**Status:** Component A.1 (Repository + Environment Setup) COMPLETE.
**Tag:** `v0.1.0-alpha` pushed to GitHub.
**Acceptance gate:** `python code/utils/verify_a1.py` exit code 0.
**Repo location (local):** `~/code/thesis-rp-migration/` (note: deviation from `A1_SETUP.md` default `~/thesis-rp-migration/`; harmless — `paths.py` resolves repo-relative).
**Repo URL:** https://github.com/akmar-sad/thesis-rp-migration
**Data root:** `/home/akmaru/thesis-data/` (DATA_ROOT in `.env`).

---

## Locked configuration confirmed

| Item | Value |
|---|---|
| OS | Windows 11 + WSL2 Ubuntu 24.04 |
| Env manager | conda (miniforge) + pip-pinned inside |
| Python | 3.11 (conda env `thesis-rp`) |
| Regridding | xesmf (installed inside WSL2) |
| FFA language | Python-only primary (lmoments3 + scipy.stats); R appendix optional |
| GEE auth | Interactive personal account via `--auth_mode=notebook`; project `ug-thesis-rs-flood-modelling`; token expires weekly |
| Data paths | Relative under DATA_ROOT in `.env` |
| Pre-commit | Enforced (black + isort + nbstripout) |
| GitHub repo | Public from Day 1; SSH key linked |
| Zenodo | `.zenodo.json` present; DOI minted automatically on submission tag |

---

## Setup deviations logged (see `docs/deviation_log.md`)

- **LIM-A1-1** — GEE personal-account interactive auth (reproducibility limitation documented per R10; reviewers must authenticate independently).
- **LIM-A1-2** — GEE auth via legacy `--auth_mode=notebook` flow after gcloud-delegated default failed with scope-mismatch errors. GCP project `ug-thesis-rs-flood-modelling` properly registered for Earth Engine. Weekly token refresh required.

No methodological deviation. No rollback condition (R-1 through R-7 in Appendix B) invoked.

---

## Open dependencies carried into Chain A

| Dependency | When needed | Status |
|---|---|---|
| BWDB SW46.9L Excel (MDD + 3-hourly WL, 1988–2026) | A.2 (next session) | In hand; structurally unaudited |
| BMD daily P national network | B.0 (Day 2–3) | Acquired; awaiting B.0 structural QC |
| CDS API registration (~/.cdsapirc) | B.0b (Day 3–4) | Not started — submit registration NOW to avoid Day-3 hold |
| ESGF account credentials | B.1 (Day 1–3) | Not started — register at https://esgf-node.llnl.gov/user/add/ |
| FFWC bulletin archive audit (1988/1998/2017/2020/2022) | A.7 (Day 6) | Not started — inventory which years are digitally recoverable |

**Action before A.2 session opens:** submit CDS API registration. Approval can take a few hours to a day; do not start the request on Day 3 morning.

---

## ★ THREE UNRESOLVED QUESTIONS — MUST BE ANSWERED AT A.2 SESSION ENTRY ★

These were raised in prior chats and remain unresolved. A.2 cannot start without written answers.

### Q1 — Forensic-only confirmation for A.2 stage–discharge diagnostic

The §14.3 A.2 spec instructs a stage–discharge internal-consistency diagnostic to flag rating-curve revision discontinuities. The rating-curve question is CLOSED per R-DATA-1. Confirm verbatim (or amend) the following written statement for the deviation log:

> "The §14.3 A.2 stage–discharge internal-consistency diagnostic is forensic-documentation only, not a rating-curve reopener. Findings are reported in Ch.4 as data-quality context. M11 binds against any rating-curve re-estimation."

**Decision required:** confirm verbatim / amend / reject.

### Q2 — AMS censoring rule

Proposed rule for annual maximum series construction at SW46.9L:

> "An AMS year is dropped if (a) >20% of JJAS days are missing, OR (b) the annual maximum candidate day falls within a gap window of ≥3 consecutive missing days."

**Decision required:** accept proposed rule / amend thresholds / propose alternative.

### Q3 — Minimum record-length floor

§14.3 D.3 states "1988–2024, 37 yr usable per delivered BWDB record." If A.2 structural audit reveals additional years require censoring under Q2's rule, what is the floor below which R-6 rollback to SW267 is triggered?

Proposed floor: **30 years** (consistent with Cannon 2015 ≥30 yr guidance for QDM calibration; Mohammed 2018 used 36 yr at Bahadurabad).

**Decision required:** accept 30 yr floor / amend threshold / propose alternative.

---

## A.2 session-entry template (use this verbatim in the next chat)

```
Component A.2 — BWDB Q/H QC + AMS extraction. Pattern P1 (first
implementation). A.1 closed 2026-05-27 at tag v0.1.0-alpha; verify_a1.py
exit 0 confirmed. BWDB SW46.9L Excel attached (or path: ~/thesis-data/
observed/<filename>.xlsx).

Decisions on three carried-forward items:
  Q1 (forensic-only diagnostic): [CONFIRM VERBATIM / AMEND / REJECT]
  Q2 (AMS censoring rule):       [ACCEPT / AMEND TO ... / PROPOSE ...]
  Q3 (minimum record floor):     [ACCEPT 30 yr / AMEND TO ... yr]

Proceed with A.2 phase-α structural audit per §14.3 A.2 spec:
sheet inventory, variable identification, units, date format,
per-year JJAS completeness, 2025–2026 truncation profile.

Acceptance gate (proposed, refine in session):
  - ≥30 yr usable record after Q2 censoring
  - All JJAS coverage tables produced
  - Stage–discharge forensic plot generated (per Q1 forensic framing)
  - AMS extracted; per-year flags documented

Deliverable paths:
  /results/baseline/A2_bwdb_qc_report.md
  /results/baseline/A2_ams_sw46p9l_<startYear>-<endYear>.csv
  /results/baseline/A2_per_year_completeness.csv
  /results/baseline/A2_stage_discharge_forensic.pdf
```

---

## Session discipline reminders for A.2

- A.2 produces analysis code (Python) — first chain to do so.
- M1 binds: A.2 cannot start until A.1 upstream verification confirmed (this log + screenshot of `verify_a1.py` exit 0 satisfies this).
- M4 binds: acceptance-gate computation code must accompany implementation.
- M6 binds: ready-to-commit code with `logging` (use `utils.logging_config.configure_logging("A.2_bwdb_qc")`), error handling, exit codes.
- A.2 has TWO phases — phase-α (structural audit of Excel), phase-β (QC + AMS extraction). Do not skip phase-α.

---

## Files to bring to A.2 session

1. This log entry (`A1_CLOSURE_2026-05-27.md`)
2. The BWDB Excel file
3. The locked system prompt (`UG_Thesis-v1-locked-edit-1.txt`)
4. A screenshot of `verify_a1.py` exit 0 (optional — for completeness)

---

*A.1 plumbing complete. Next session: real work begins.*
